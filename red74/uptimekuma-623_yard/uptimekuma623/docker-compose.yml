
networks:
  cfnet1:
    name: cfnet1
  cfnet_r74_daveg2online:
    name: cfnet_r74_daveg2online

volumes:
  uptime-kuma-data:
  #uptime-db-data:


services:

  upkuma:
    # http://10.33.44.81:6175/
    image: louislam/uptime-kuma:latest
    restart: unless-stopped
    ports:
      - "6175:3001"
    volumes:
      - ./sysdata/uptime-kuma-data:/app/data
    networks:
      - default
      - cfnet_r74_daveg2online
    # env_file:
      # - .env 
    # depends_on:
      # - dbm
